import React from 'react';
import SectionHeader from '../common/SectionHeader';

const ProfileHeader = () => {
    return (
        <SectionHeader
            label="01 — Profile"
        />
    );
};

export default ProfileHeader;
